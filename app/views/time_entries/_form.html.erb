<%= form_for(@time_entry) do |f| %>
  <% if @time_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@time_entry.errors.count, "error") %> prohibited this time_entry from being saved:</h2>

      <ul>
      <% @time_entry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :user_id, value: current_user.id %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>

  <div class="field">
    <%= f.label :task_id %><br>
    <%= f.collection_select :task_id, @tasks, :id, :fully_qualified_name %>
  </div>

  <div class="field">
    <%= f.label :seconds %><br>
    <%= f.text_field :seconds %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
